"""The colours a calendar may be, named rather than written out.

A key rather than a hex value, so the app decides what "blue" looks like on a
screen and an old row never carries a colour from a theme that has moved on.
The web app keeps the same list beside its own stylesheet.
"""

from __future__ import annotations

PALETTE = ("blue", "violet", "orange", "coral", "gold", "teal", "pink", "slate")
