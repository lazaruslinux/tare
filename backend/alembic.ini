[alembic]
script_location = migrations
prepend_sys_path = .
path_separator = os
# Left empty on purpose. The environment reads the URL from app settings, so
# there is one place credentials come from. Tests set this option in memory to
# point a migration run at a scratch database.
sqlalchemy.url =
