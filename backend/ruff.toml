target-version = "py313"
line-length = 100

[lint]
# The correctness set only, named explicitly. Ruff's defaults move between
# releases, and a lint set that changes under a pinned codebase turns an
# upgrade into a diff.
select = ["E4", "E7", "E9", "F"]
