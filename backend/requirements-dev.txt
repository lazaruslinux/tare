# Test and lint tooling. Never installed into the runtime image.
# httpx is pinned to the runtime lock's version so installing this file on top
# of requirements.txt cannot move it.
pytest==9.1.1
httpx==0.28.1
ruff==0.16.6
mypy==2.3.1
